# german-vocab — environment variables
#
# This project uses a local SQLite database (data/vocab.db) and needs no
# external database or API keys.
#
# Optional: customize the data directory (defaults to ./data)
# DATA_DIR=./data
